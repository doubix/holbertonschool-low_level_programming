#include "holberton.h"
/**
 * main - entry point
 * dsiplaying a msg
 * Return: Always 0 (Success)
 */
int _abs(int n)
{
    if (n >= 0)
    {
        return (n);
    } else
    {
        return (-n);
    }
}